spring:
  datasource:
    url: jdbc:mysql://localhost:3306/banking_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  thymeleaf:
    cache: false

server:
  port: 8080